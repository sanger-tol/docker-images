default:
  image:
    name: ubuntu:18.04

stages:
  - test

test_image:
  stage: test
  tags:
    - openstack-autoscale-theta-docker-in-docker
  script:
    - echo $APPLICATION_NAME
    - ./test.sh $APPLICATION_NAME
  rules:
    - if: '$APPLICATION_NAME != ""'
